Any software pehle RAM me aata hai tabhi chal pata hai ,RAM permanently kuch store nahi karta hai 
and ise humari machine ki primary memory bhi kehte hai .Ram is made up of lakhs of
capacitor and each capacitor has only two states charge hai yaa charge nahi hai , charge hai ko 1 and charge nahi ko 0 .5 means 101 in binary and 101 means ⏹◻⏹ on-off-on yaani charge-nocharge-charge.



---------------------------------------The full process How a calculator software runs on computer
Developer writes code
        ↓
Compiler creates executable
        ↓
calculator.exe stored on SSD
        ↓
User double-clicks
        ↓
OS creates process
        ↓
OS loads program into RAM
        ↓
CPU fetches instructions from RAM
        ↓
CPU executes instructions
        ↓
Program asks OS for window
        ↓
OS talks to GPU
        ↓
GPU draws pixels
        ↓
Monitor shows Calculator
        ↓
User clicks button
        ↓
OS sends event
        ↓
Calculator handles event
        ↓
CPU calculates result
        ↓
GPU redraws screen
        ↓
User sees answer

---------------------------------------The full process How a calculator software runs on computer